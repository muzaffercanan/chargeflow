apiVersion: apps/v1
kind: Deployment
metadata:
  name: session-service
  labels:
    app.kubernetes.io/name: session-service
    app.kubernetes.io/part-of: chargesquare
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: session-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: session-service
        app.kubernetes.io/part-of: chargesquare
    spec:
      containers:
        - name: session-service
          image: chargesquare/session-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8082
          envFrom:
            - configMapRef:
                name: chargesquare-config
          env:
            - name: SESSION_DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: chargesquare-postgres
                  key: username
            - name: SESSION_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: chargesquare-postgres
                  key: password
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: http
            initialDelaySeconds: 20
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
